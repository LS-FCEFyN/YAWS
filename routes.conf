# YAWS route table.
# In case you did not read the source code, here is how the route table works:
# Format: <request path> = <target path>, one per line.
# Blank lines and lines starting with '#' are ignored.

/ = /index.html